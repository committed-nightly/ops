name: CI

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Test the shift watcher's notifier
        run: python3 scripts/test_notify_shift_outcome.py
